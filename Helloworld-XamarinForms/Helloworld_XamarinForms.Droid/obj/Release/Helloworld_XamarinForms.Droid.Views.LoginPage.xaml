<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
        x:Class="Helloworld_XamarinForms.Shared.Views.LoginPage" Title="Login Page">
        <ContentPage.Content>
          <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="30">
                                <Entry BackgroundColor="Gray" Placeholder="Enter Username" 
                                        WidthRequest = "300"  Text="{Binding UserName}" x:Name="userNameEntry" />
                                <Entry BackgroundColor="Gray" Placeholder="Enter Password" 
                                        WidthRequest = "300"  Text="{Binding Password}" x:Name="passwordEntry" />
                                <Button Text="Login" Command="{Binding LoginCommand}"/>
                               
                        </StackLayout>

                        <StackLayout IsVisible="{Binding IsBusy}" BackgroundColor="#80000000" Padding="12"
                                  AbsoluteLayout.LayoutFlags="PositionProportional"
                                  AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1">
                                <ActivityIndicator IsRunning="{Binding IsBusy}" Color ="White">
                                </ActivityIndicator>
                                <Label Text="Authenticating..." HorizontalOptions="Center" TextColor="White"/>
                        </StackLayout>
                </AbsoluteLayout>
        </ContentPage.Content>
</ContentPage>
