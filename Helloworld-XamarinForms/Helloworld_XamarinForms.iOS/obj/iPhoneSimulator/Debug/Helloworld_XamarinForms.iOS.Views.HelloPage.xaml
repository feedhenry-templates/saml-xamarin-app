<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
        x:Class="Helloworld_XamarinForms.Shared.Views.HelloPage" Title="Hello Page">
        <ContentPage.Content>
                <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="30">
                                <Entry HeightRequest="40" BackgroundColor="Gray" Placeholder="Enter Message Here" WidthRequest = "300"  Text="{Binding Message}" x:Name="helloMessageEntry" />
                                <Button Text="Call Service" Command="{Binding HelloWorldCommand}"/>
                                <Label HeightRequest="40" BackgroundColor="Gray" Text="{Binding ResponseMsg}" x:Name="serviceResponselbl" WidthRequest = "300"/>
                        </StackLayout>

                        <StackLayout IsVisible="{Binding IsBusy}" BackgroundColor="#80000000" Padding="12"
                                  AbsoluteLayout.LayoutFlags="PositionProportional"
                                  AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1">
                                <ActivityIndicator IsRunning="{Binding IsBusy}" Color ="White">
                                </ActivityIndicator>
                                <Label Text="Calling Service..." HorizontalOptions="Center" TextColor="White"/>
                        </StackLayout>
                </AbsoluteLayout>
        </ContentPage.Content>
</ContentPage>
